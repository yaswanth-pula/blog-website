<%- include('partials/header'); %>
    <h1>Home</h1>
    <p><%= homeStartingContent %> </p>
    <!-- already used foreach in challenge 13  -->
    <% htmlPosts.forEach(htmlPost => { %>
            <% console.log(htmlPost.title) %> 
    <% }) %>
<%- include('partials/footer'); %>